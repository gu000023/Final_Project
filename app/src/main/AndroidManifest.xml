<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.androidlabs">

    <uses-permission android:name="android.permission.INTERNET" /> <!-- lab 6 watch line below -->
    <uses-feature android:name="android.hardware.type.watch" />
    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_namee"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ListViewForImage"/>
        <activity android:name=".MyOpener"/>
        <activity android:name=".NasaDB"/>
        <activity android:name=".NasaImage"/>
        <activity android:name=".EnterGeoInfo"/>
        <activity android:name=".EmptyActivity"/>
        <activity android:name=".NasaFragment"/>
        <activity android:name=".MainActivityfp">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>